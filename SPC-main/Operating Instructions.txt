1.  把整个 `SPC-main` 文件夹上传到你的 Ubuntu 服务器（PAI-DSW）。
2.  在服务器终端中，进入项目目录：
    ```bash
    cd SPC-main
    ```
3.  运行一键脚本：
    ```bash
    bash setup_env.sh
    ```
    （这个脚本会自动安装所有依赖，过程可能需要几分钟）

4.  安装完成后，直接跑评估：
    ```bash
    python eval/infer_batch.py
    ```